import * as React from 'react'
import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	image: {
		flex: 1,
		justifyContent: 'flex-start',
		width: '100%',
		marginTop: 0,
		padding: 0,
	},
	textBox: { display: 'flex', justifyContent: 'space-evenly', height: '100%' },
})
